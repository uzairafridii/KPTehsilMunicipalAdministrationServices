<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">


    <ImageView
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:src="@drawable/ic_launcher_background"
        android:id="@+id/selectedImages"
        android:scaleType="fitXY"
        android:layout_marginLeft="@dimen/reycler_view_margin"
        android:layout_marginRight="@dimen/reycler_view_margin"
        android:layout_marginTop="@dimen/edit_margin"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        />

    <ImageButton
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:id="@+id/imageExitButton"
        android:clickable="true"
        android:foreground="?android:attr/selectableItemBackground"
        android:background="@drawable/bg_for_exit_image_btn"
        android:layout_marginLeft="@dimen/image_btn_margin_top"
        android:layout_marginRight="@dimen/image_btn_margin_top"
        android:layout_marginTop="@dimen/edit_padding_left"
        android:padding="2dp"
        android:src="@drawable/ic_close_black_24dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        />



</androidx.constraintlayout.widget.ConstraintLayout>